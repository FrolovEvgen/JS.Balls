/* Start game block. */
#start-block {
  width: inherit;
  height: 430px;
  position: relative;
  /* Start button */
  button {
    font-size: 18px;
    padding: 10px;
    position: absolute;
    top: 190px;
    left: 220px;
    width: 200px;
    height: 50px;
  }
}